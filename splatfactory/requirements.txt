# Core dependencies for the splatfactory training/eval package (PEP 508, no poetry).
# The two CUDA extensions need a built torch + nvcc and must be installed with
# --no-build-isolation, so they are NOT listed here. See the "Training" section in the README:
#   pip install --no-build-isolation --use-pep517 \
#       git+https://github.com/JoannaCCJH/gsplat.git \
#       git+https://github.com/rahul-goel/fused-ssim/
torch
torchvision
numpy<2
einops
omegaconf
hydra-core
h5py
pandas
tqdm
matplotlib
opencv-python
albumentations
chamferdist
lpips
imageio[ffmpeg]
plotly
safetensors
scikit-image
scikit-learn
tabulate
tensordict
wandb
huggingface-hub
pillow
depth-anything-3 @ git+https://github.com/bytedance-seed/depth-anything-3.git
